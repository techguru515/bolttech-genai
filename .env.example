# Optional: enable live LLM calls (OpenAI-compatible API)
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini

# Set to "mock" to force template-based GenAI (default when no API key)
GENAI_MODE=auto

MLFLOW_TRACKING_URI=./mlruns
